# Foundation Specification

The goal is to implement the core logic for map generation, hero statistics, and the deterministic simulation loop.

## Requirements
1. Implement `DungeonMap` generation logic based on hex signatures.
2. Define `Hero` class with stats derived from long-term user history.
3. Create basic deterministic simulation loop.
